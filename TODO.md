# TODO
- [x] Fix checked_files behaviour. 1 chapter files should not be added to the list before they have been fixed.
  This currently happens in scan mode.
  Check that it doesn't happen in other scenarios too, in case of error for example.
- Log how long a scan run takes.
- Log ffmpeg version on startup. 
